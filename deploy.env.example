# Конфиг деплоя. Скопируй в deploy.env (он в .gitignore) и заполни.
# deploy.sh подхватывает эти переменные автоматически.

DEPLOY_HOST=root@YOUR_SERVER_IP
DEPLOY_PORT=22
DEPLOY_DIR=/opt/travelmate
VITE_API_URL=https://trip-radar.ru
